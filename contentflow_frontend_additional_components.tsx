// frontend/src/components/ContentPreview.tsx\n/**\n * ContentPreview Component\n * Displays generated content for all platforms\n */\n\nimport React, { useState } from 'react'\nimport { motion, AnimatePresence } from 'framer-motion'\nimport { Download, Share2, Copy, ArrowLeft, CheckCircle } from 'lucide-react'\nimport type { ProcessingJob } from '../types'\n\ninterface ContentPreviewProps {\n  job: ProcessingJob\n  onBack: () => void\n}\n\nconst ContentPreview: React.FC<ContentPreviewProps> = ({ job, onBack }) => {\n  const [activeTab, setActiveTab] = useState<string>('linkedin_carousel')\n  const [copied, setCopied] = useState(false)\n\n  const contentTabs = [\n    { id: 'linkedin_carousel', label: '💼 LinkedIn', icon: 'linkedin' },\n    { id: 'twitter_thread', label: '🐦 Twitter', icon: 'twitter' },\n    { id: 'blog_post', label: '📝 Blog', icon: 'blog' },\n  ]\n\n  const handleCopy = (text: string) => {\n    navigator.clipboard.writeText(text)\n    setCopied(true)\n    setTimeout(() => setCopied(false), 2000)\n  }\n\n  return (\n    <motion.div\n      className=\"space-y-6\"\n      initial={{ opacity: 0 }}\n      animate={{ opacity: 1 }}\n    >\n      {/* Header with back button */}\n      <motion.div\n        className=\"flex items-center justify-between\"\n        initial={{ x: -20 }}\n        animate={{ x: 0 }}\n      >\n        <button\n          onClick={onBack}\n          className=\"flex items-center gap-2 text-slate-400 hover:text-emerald-400 transition-colors\"\n        >\n          <ArrowLeft className=\"w-5 h-5\" />\n          Back\n        </button>\n        <motion.div\n          className=\"flex items-center gap-2 text-emerald-400\"\n          initial={{ scale: 0.9 }}\n          animate={{ scale: 1 }}\n        >\n          <CheckCircle className=\"w-5 h-5\" />\n          <span className=\"font-semibold\">Generation Complete!</span>\n        </motion.div>\n      </motion.div>\n\n      {/* Tabs */}\n      <motion.div\n        className=\"flex gap-2 border-b border-slate-700/50 overflow-x-auto\"\n        initial={{ y: 10 }}\n        animate={{ y: 0 }}\n      >\n        {contentTabs.map(tab => (\n          <button\n            key={tab.id}\n            onClick={() => setActiveTab(tab.id)}\n            className={`px-4 py-3 font-medium transition-all whitespace-nowrap border-b-2 ${\n              activeTab === tab.id\n                ? 'text-emerald-400 border-emerald-500'\n                : 'text-slate-400 border-transparent hover:text-slate-300'\n            }`}\n          >\n            {tab.label}\n          </button>\n        ))}\n      </motion.div>\n\n      {/* Content display area */}\n      <AnimatePresence mode=\"wait\">\n        <motion.div\n          key={activeTab}\n          className=\"bg-slate-900/50 backdrop-blur-xl border border-slate-700/50 rounded-2xl p-8 space-y-4\"\n          initial={{ opacity: 0, y: 10 }}\n          animate={{ opacity: 1, y: 0 }}\n          exit={{ opacity: 0, y: -10 }}\n        >\n          {/* LinkedIn Carousel */}\n          {activeTab === 'linkedin_carousel' && (\n            <div className=\"space-y-4\">\n              <h3 className=\"text-lg font-semibold text-white\">LinkedIn Carousel (5 slides)</h3>\n              {Array.from({ length: 5 }).map((_, i) => (\n                <motion.div\n                  key={i}\n                  className=\"bg-slate-800/50 border border-slate-700/50 rounded-lg p-4 space-y-3\"\n                  initial={{ opacity: 0, x: -20 }}\n                  animate={{ opacity: 1, x: 0 }}\n                  transition={{ delay: i * 0.1 }}\n                >\n                  <div className=\"text-sm font-medium text-emerald-400\">Slide {i + 1}</div>\n                  <p className=\"text-slate-300 text-sm line-clamp-4\">\n                    This is sample generated content for slide {i + 1}. In production, this would be actual AI-generated content.\n                  </p>\n                  <button\n                    onClick={() => handleCopy(`Slide ${i + 1} content`)}\n                    className=\"flex items-center gap-2 text-xs text-slate-400 hover:text-emerald-400 transition-colors\"\n                  >\n                    <Copy className=\"w-4 h-4\" />\n                    Copy\n                  </button>\n                </motion.div>\n              ))}\n            </div>\n          )}\n\n          {/* Twitter Thread */}\n          {activeTab === 'twitter_thread' && (\n            <div className=\"space-y-3\">\n              <h3 className=\"text-lg font-semibold text-white\">Twitter Thread (5 tweets)</h3>\n              {Array.from({ length: 5 }).map((_, i) => (\n                <motion.div\n                  key={i}\n                  className=\"bg-slate-800/50 border border-slate-700/50 rounded-lg p-4 space-y-2\"\n                  initial={{ opacity: 0, x: -20 }}\n                  animate={{ opacity: 1, x: 0 }}\n                  transition={{ delay: i * 0.1 }}\n                >\n                  <div className=\"text-sm font-medium text-cyan-400\">{i + 1}/{5}</div>\n                  <p className=\"text-slate-300 text-sm\">Sample tweet {i + 1}. This demonstrates how the AI would generate engaging Twitter content...</p>\n                  <button\n                    onClick={() => handleCopy(`Tweet ${i + 1}`)}\n                    className=\"flex items-center gap-2 text-xs text-slate-400 hover:text-cyan-400 transition-colors\"\n                  >\n                    <Copy className=\"w-4 h-4\" />\n                    Copy\n                  </button>\n                </motion.div>\n              ))}\n            </div>\n          )}\n\n          {/* Blog Post */}\n          {activeTab === 'blog_post' && (\n            <div className=\"space-y-4 max-h-[600px] overflow-y-auto\">\n              <div className=\"space-y-2\">\n                <h2 className=\"text-2xl font-bold text-white\">Blog Post Title</h2>\n                <p className=\"text-slate-400 text-sm\">Posted on {new Date().toLocaleDateString()}</p>\n              </div>\n              <p className=\"text-slate-300 leading-relaxed text-sm\">\n                This is a sample blog post generated from the source content. In production, this would be a full-length blog post (800-1000 words) with multiple sections, engaging narrative, and a strong call-to-action.\n              </p>\n              <button\n                onClick={() => handleCopy('Full blog post content')}\n                className=\"flex items-center gap-2 text-sm text-slate-400 hover:text-emerald-400 transition-colors\"\n              >\n                <Copy className=\"w-4 h-4\" />\n                Copy Full Post\n              </button>\n            </div>\n          )}\n        </motion.div>\n      </AnimatePresence>\n\n      {/* Action buttons */}\n      <motion.div\n        className=\"flex flex-col sm:flex-row gap-3\"\n        initial={{ opacity: 0, y: 10 }}\n        animate={{ opacity: 1, y: 0 }}\n        transition={{ delay: 0.2 }}\n      >\n        <button className=\"flex-1 flex items-center justify-center gap-2 px-6 py-3 rounded-lg bg-emerald-500/20 border border-emerald-500/50 text-emerald-400 hover:bg-emerald-500/30 transition-colors\">\n          <Download className=\"w-5 h-5\" />\n          Export All\n        </button>\n        <button className=\"flex-1 flex items-center justify-center gap-2 px-6 py-3 rounded-lg bg-slate-800/50 border border-slate-700/50 text-slate-300 hover:text-emerald-400 transition-colors\">\n          <Share2 className=\"w-5 h-5\" />\n          Share\n        </button>\n      </motion.div>\n\n      {/* Copy notification */}\n      <AnimatePresence>\n        {copied && (\n          <motion.div\n            className=\"fixed bottom-6 right-6 flex items-center gap-2 px-4 py-3 rounded-lg bg-emerald-500/20 border border-emerald-500/50 text-emerald-400\"\n            initial={{ opacity: 0, y: 10 }}\n            animate={{ opacity: 1, y: 0 }}\n            exit={{ opacity: 0, y: 10 }}\n          >\n            <CheckCircle className=\"w-4 h-4\" />\n            Copied to clipboard\n          </motion.div>\n        )}\n      </AnimatePresence>\n    </motion.div>\n  )\n}\n\nexport default ContentPreview\n\n// ============================================================================\n// frontend/src/components/AnimatedBackground.tsx\n// ============================================================================\n\n/**\n * AnimatedBackground Component\n * Provides animated gradient background with floating elements\n */\n\nconst AnimatedBackground: React.FC = () => {\n  return (\n    <div className=\"absolute inset-0 overflow-hidden\">\n      {/* Gradient background */}\n      <div className=\"absolute inset-0 bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950\" />\n\n      {/* Animated gradient orbs */}\n      <motion.div\n        className=\"absolute top-1/4 left-1/4 w-96 h-96 bg-emerald-500/30 rounded-full mix-blend-screen filter blur-3xl\"\n        animate={{\n          x: [0, 50, -30, 0],\n          y: [0, -50, 30, 0],\n        }}\n        transition={{\n          duration: 15,\n          repeat: Infinity,\n          ease: 'easeInOut',\n        }}\n      />\n\n      <motion.div\n        className=\"absolute bottom-1/4 right-1/4 w-96 h-96 bg-cyan-500/30 rounded-full mix-blend-screen filter blur-3xl\"\n        animate={{\n          x: [0, -50, 30, 0],\n          y: [0, 50, -30, 0],\n        }}\n        transition={{\n          duration: 18,\n          repeat: Infinity,\n          ease: 'easeInOut',\n        }}\n      />\n\n      {/* Grid pattern overlay */}\n      <div\n        className=\"absolute inset-0 opacity-5\"\n        style={{\n          backgroundImage: `\n            linear-gradient(0deg, transparent 24%, rgba(255,255,255,.05) 25%, rgba(255,255,255,.05) 26%, transparent 27%, transparent 74%, rgba(255,255,255,.05) 75%, rgba(255,255,255,.05) 76%, transparent 77%, transparent),\n            linear-gradient(90deg, transparent 24%, rgba(255,255,255,.05) 25%, rgba(255,255,255,.05) 26%, transparent 27%, transparent 74%, rgba(255,255,255,.05) 75%, rgba(255,255,255,.05) 76%, transparent 77%, transparent)\n          `,\n          backgroundSize: '50px 50px',\n        }}\n      />\n\n      {/* Radial gradient vignette */}\n      <div className=\"absolute inset-0 bg-radial-gradient from-transparent via-transparent to-slate-950/40\" />\n    </div>\n  )\n}\n\nexport default AnimatedBackground\n