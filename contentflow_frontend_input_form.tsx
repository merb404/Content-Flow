// frontend/src/components/InputForm.tsx
/**\n * InputForm Component\n * Collects user input for content source URL and generation preferences\n */\n\nimport React, { useState } from 'react'\nimport { motion } from 'framer-motion'\nimport { LinkIcon, Zap, Settings2 } from 'lucide-react'\n\ninterface InputFormProps {\n  onSubmit: (data: FormData) => void\n  isLoading?: boolean\n}\n\ninterface FormData {\n  source_url: string\n  source_type: 'youtube' | 'article' | 'podcast' | 'transcript'\n  content_types: string[]\n  tone: string\n  target_audience?: string\n  include_hashtags: boolean\n  include_emojis: boolean\n}\n\nconst InputForm: React.FC<InputFormProps> = ({ onSubmit, isLoading = false }) => {\n  const [formData, setFormData] = useState<FormData>({\n    source_url: '',\n    source_type: 'youtube',\n    content_types: ['linkedin_carousel', 'twitter_thread'],\n    tone: 'professional',\n    target_audience: '',\n    include_hashtags: true,\n    include_emojis: false,\n  })\n  const [showAdvanced, setShowAdvanced] = useState(false)\n  const [error, setError] = useState<string | null>(null)\n\n  const contentTypeOptions = [\n    { id: 'linkedin_carousel', label: 'LinkedIn Carousel', icon: '💼' },\n    { id: 'twitter_thread', label: 'Twitter Thread', icon: '🐦' },\n    { id: 'blog_post', label: 'Blog Post', icon: '📝' },\n    { id: 'instagram_caption', label: 'Instagram Caption', icon: '📸' },\n    { id: 'newsletter', label: 'Newsletter', icon: '📧' },\n  ]\n\n  const toneOptions = [\n    { id: 'professional', label: 'Professional' },\n    { id: 'casual', label: 'Casual' },\n    { id: 'inspirational', label: 'Inspirational' },\n    { id: 'educational', label: 'Educational' },\n  ]\n\n  const handleSubmit = (e: React.FormEvent) => {\n    e.preventDefault()\n    setError(null)\n\n    // Validation\n    if (!formData.source_url.trim()) {\n      setError('Please enter a URL')\n      return\n    }\n\n    if (formData.content_types.length === 0) {\n      setError('Please select at least one content type')\n      return\n    }\n\n    // Validate URL format\n    try {\n      new URL(formData.source_url)\n    } catch {\n      setError('Please enter a valid URL')\n      return\n    }\n\n    onSubmit(formData)\n  }\n\n  const toggleContentType = (contentType: string) => {\n    setFormData(prev => ({\n      ...prev,\n      content_types: prev.content_types.includes(contentType)\n        ? prev.content_types.filter(ct => ct !== contentType)\n        : [...prev.content_types, contentType],\n    }))\n  }\n\n  const containerVariants = {\n    hidden: { opacity: 0 },\n    visible: {\n      opacity: 1,\n      transition: {\n        staggerChildren: 0.1,\n        delayChildren: 0.1,\n      },\n    },\n  }\n\n  const itemVariants = {\n    hidden: { opacity: 0, y: 10 },\n    visible: {\n      opacity: 1,\n      y: 0,\n      transition: { duration: 0.3 },\n    },\n  }\n\n  return (\n    <motion.div\n      className=\"relative\"\n      variants={containerVariants}\n      initial=\"hidden\"\n      animate=\"visible\"\n    >\n      {/* Background card */}\n      <div className=\"absolute inset-0 bg-gradient-to-br from-emerald-500/10 to-cyan-500/10 rounded-2xl blur-xl\" />\n\n      <motion.form\n        onSubmit={handleSubmit}\n        className=\"relative bg-slate-900/50 backdrop-blur-xl border border-slate-700/50 rounded-2xl p-8 lg:p-12 space-y-8\"\n        variants={itemVariants}\n      >\n        {/* URL Input */}\n        <motion.div className=\"space-y-3\" variants={itemVariants}>\n          <label className=\"block text-sm font-medium text-slate-300 flex items-center gap-2\">\n            <LinkIcon className=\"w-4 h-4 text-emerald-400\" />\n            Content Source URL\n          </label>\n          <motion.input\n            type=\"url\"\n            placeholder=\"https://www.youtube.com/watch?v=... or https://article.com\"\n            value={formData.source_url}\n            onChange={e => setFormData(prev => ({ ...prev, source_url: e.target.value }))}\n            className=\"w-full px-4 py-3 bg-slate-800/50 border border-slate-600/50 rounded-lg text-white placeholder-slate-500 focus:outline-none focus:border-emerald-500/50 focus:ring-2 focus:ring-emerald-500/20 transition-all duration-200\"\n            whileFocus={{ scale: 1.01 }}\n          />\n          <p className=\"text-xs text-slate-500\">Supports YouTube, articles, podcasts, and transcripts</p>\n        </motion.div>\n\n        {/* Source Type Selection */}\n        <motion.div className=\"space-y-3\" variants={itemVariants}>\n          <label className=\"block text-sm font-medium text-slate-300\">Source Type</label>\n          <div className=\"grid grid-cols-2 sm:grid-cols-4 gap-3\">\n            {(['youtube', 'article', 'podcast', 'transcript'] as const).map(type => (\n              <motion.button\n                key={type}\n                type=\"button\"\n                onClick={() => setFormData(prev => ({ ...prev, source_type: type }))}\n                className={`px-4 py-3 rounded-lg font-medium text-sm transition-all duration-200 border ${\n                  formData.source_type === type\n                    ? 'bg-emerald-500/20 border-emerald-500/50 text-emerald-300'\n                    : 'bg-slate-800/30 border-slate-600/30 text-slate-400 hover:border-slate-600/50'\n                }`}\n                whileHover={{ scale: 1.05 }}\n                whileTap={{ scale: 0.95 }}\n              >\n                {type.charAt(0).toUpperCase() + type.slice(1)}\n              </motion.button>\n            ))}\n          </div>\n        </motion.div>\n\n        {/* Content Type Selection */}\n        <motion.div className=\"space-y-3\" variants={itemVariants}>\n          <label className=\"block text-sm font-medium text-slate-300\">Content Types</label>\n          <div className=\"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3\">\n            {contentTypeOptions.map(option => (\n              <motion.button\n                key={option.id}\n                type=\"button\"\n                onClick={() => toggleContentType(option.id)}\n                className={`px-4 py-3 rounded-lg font-medium text-sm transition-all duration-200 border flex items-center gap-2 ${\n                  formData.content_types.includes(option.id)\n                    ? 'bg-emerald-500/20 border-emerald-500/50 text-emerald-300'\n                    : 'bg-slate-800/30 border-slate-600/30 text-slate-400 hover:border-slate-600/50'\n                }`}\n                whileHover={{ scale: 1.05 }}\n                whileTap={{ scale: 0.95 }}\n              >\n                <span>{option.icon}</span>\n                {option.label}\n              </motion.button>\n            ))}\n          </div>\n        </motion.div>\n\n        {/* Tone & Basic Settings */}\n        <motion.div className=\"grid grid-cols-1 sm:grid-cols-2 gap-6\" variants={itemVariants}>\n          <div className=\"space-y-3\">\n            <label className=\"block text-sm font-medium text-slate-300\">Tone</label>\n            <select\n              value={formData.tone}\n              onChange={e => setFormData(prev => ({ ...prev, tone: e.target.value }))}\n              className=\"w-full px-4 py-2 bg-slate-800/50 border border-slate-600/50 rounded-lg text-slate-300 focus:outline-none focus:border-emerald-500/50 transition-all duration-200\"\n            >\n              {toneOptions.map(tone => (\n                <option key={tone.id} value={tone.id}>\n                  {tone.label}\n                </option>\n              ))}\n            </select>\n          </div>\n          <div className=\"space-y-3\">\n            <label className=\"block text-sm font-medium text-slate-300\">Audience (Optional)</label>\n            <input\n              type=\"text\"\n              placeholder=\"e.g., Marketing professionals\"\n              value={formData.target_audience || ''}\n              onChange={e => setFormData(prev => ({ ...prev, target_audience: e.target.value }))}\n              className=\"w-full px-4 py-2 bg-slate-800/50 border border-slate-600/50 rounded-lg text-slate-300 placeholder-slate-600 focus:outline-none focus:border-emerald-500/50 transition-all duration-200\"\n            />\n          </div>\n        </motion.div>\n\n        {/* Advanced Options Toggle */}\n        <motion.button\n          type=\"button\"\n          onClick={() => setShowAdvanced(!showAdvanced)}\n          className=\"flex items-center gap-2 text-sm text-slate-400 hover:text-slate-300 transition-colors\"\n          whileHover={{ x: 5 }}\n        >\n          <Settings2 className=\"w-4 h-4\" />\n          {showAdvanced ? 'Hide' : 'Show'} Advanced Options\n        </motion.button>\n\n        {/* Advanced Options */}\n        {showAdvanced && (\n          <motion.div\n            className=\"space-y-4 pt-4 border-t border-slate-700/50\"\n            initial={{ opacity: 0, height: 0 }}\n            animate={{ opacity: 1, height: 'auto' }}\n            exit={{ opacity: 0, height: 0 }}\n          >\n            <label className=\"flex items-center gap-3 cursor-pointer\">\n              <input\n                type=\"checkbox\"\n                checked={formData.include_hashtags}\n                onChange={e => setFormData(prev => ({ ...prev, include_hashtags: e.target.checked }))}\n                className=\"w-4 h-4 rounded border-slate-600/50 bg-slate-800/50 text-emerald-500 focus:ring-emerald-500/20\"\n              />\n              <span className=\"text-sm text-slate-400\">Include hashtags</span>\n            </label>\n            <label className=\"flex items-center gap-3 cursor-pointer\">\n              <input\n                type=\"checkbox\"\n                checked={formData.include_emojis}\n                onChange={e => setFormData(prev => ({ ...prev, include_emojis: e.target.checked }))}\n                className=\"w-4 h-4 rounded border-slate-600/50 bg-slate-800/50 text-emerald-500 focus:ring-emerald-500/20\"\n              />\n              <span className=\"text-sm text-slate-400\">Include emojis</span>\n            </label>\n          </motion.div>\n        )}\n\n        {/* Error Message */}\n        {error && (\n          <motion.div\n            className=\"p-4 rounded-lg bg-red-500/10 border border-red-500/30 text-red-300 text-sm\"\n            initial={{ opacity: 0, y: -10 }}\n            animate={{ opacity: 1, y: 0 }}\n          >\n            {error}\n          </motion.div>\n        )}\n\n        {/* Submit Button */}\n        <motion.button\n          type=\"submit\"\n          disabled={isLoading}\n          className=\"w-full px-6 py-3 rounded-lg font-semibold bg-gradient-to-r from-emerald-500 to-cyan-500 text-white flex items-center justify-center gap-2 hover:shadow-lg hover:shadow-emerald-500/50 disabled:opacity-50 disabled:cursor-not-allowed transition-all duration-200\"\n          whileHover={{ scale: 1.02 }}\n          whileTap={{ scale: 0.98 }}\n        >\n          {isLoading ? (\n            <>\n              <motion.div\n                className=\"w-4 h-4 border-2 border-white border-t-transparent rounded-full\"\n                animate={{ rotate: 360 }}\n                transition={{ duration: 1, repeat: Infinity }}\n              />\n              Processing...\n            </>\n          ) : (\n            <>\n              <Zap className=\"w-5 h-5\" />\n              Generate Content\n            </>\n          )}\n        </motion.button>\n      </motion.form>\n    </motion.div>\n  )\n}\n\nexport default InputForm
